{
  "name": "lensing",
  "description": "On-device semantic focus for social feeds.",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build && wxt build -b firefox",
    "build:firefox": "wxt build -b firefox",
    "zip": "wxt zip",
    "zip:firefox": "wxt zip -b firefox",
    "compile": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "postinstall": "node scripts/sync-ort.mjs && wxt prepare",
    "sync:ort": "node scripts/sync-ort.mjs",
    "watch": "node scripts/watch-build.mjs",
    "test:model": "vitest run --config vitest.model.config.ts",
    "build:debug": "VITE_LENSING_DEBUG=1 wxt build",
    "build:debug:firefox": "VITE_LENSING_DEBUG=1 wxt build -b firefox",
    "build:wasm": "VITE_LENSING_DEBUG=1 VITE_LENSING_BACKEND=wasm wxt build"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.7.6",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.3",
    "happy-dom": "^20.14.3",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wxt": "^0.20.12"
  }
}
